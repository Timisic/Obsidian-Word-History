# Context Snapshot — obsidian-word-history-tool

- **Task statement:** 为本地 Obsidian vault（`/Users/hong/Obsidian Notes`）做一个简洁、稳定、可持续使用的本地工具，从 Git 历史重建字数变化，并生成类似 Star History 风格的趋势图。
- **Desired outcome:** 第一版采用本地 CLI + HTML 报告方案，能提取历史并展示可查看的图表。
- **Stated solution:** 在 `Downloads` 下新建项目目录，做一个本地程序；字数统计口径尽量与当前 `novel-word-count` 插件一致；风格参考 Star History；功能保持简洁。
- **Probable intent hypothesis:** 用户想获得长期可复用的个人写作趋势视图，而不是一次性的分析脚本；更看重稳定、简单、可持续使用，而不是功能堆叠。
- **Known facts / evidence:**
  - Vault 路径：`/Users/hong/Obsidian Notes`
  - 本地 `.git` 存在，commit 数 418，时间跨度 2025-01-16 到 2026-04-12
  - 按提交日聚合的日级趋势可重建
  - 未发现 `obsidian-daily-stats` / `keep-the-rhythm` / `version-control` 的本地历史统计文件
  - 已安装 `novel-word-count`，其 `data.json` 提供当前字数缓存和部分统计配置
  - `novel-word-count` 当前配置中 `excludeCodeBlocks=false`、`excludeComments=false`、`excludeFootnotes=false`、`excludeNonVisibleLinkPortions=false`
  - 用户要求：字数统计口径与当前 word count 尽量一致；第一版不要太复杂；风格接近 Star History
- **Constraints:**
  - 优先本地方案，不依赖 GitHub 远端作为首选数据源
  - 第一版保持简洁、稳定、可重复使用
  - 需要规划先行，暂不直接实现
- **Unknowns / open questions:**
  - 第一版明确的 non-goals 是什么
  - 报告交付形态边界：仅单页 HTML，还是同时导出 CSV/JSON
  - 是否需要多图联动 / 多视图切换，还是单页固定布局
  - “单篇笔记统计”在第一版中是 Top N 榜单、可选明细页，还是交互 drill-down
- **Decision-boundary unknowns:**
  - OMX 在技术栈、目录命名、图表库、数据缓存格式上可自行决定到什么程度
  - 是否允许第一版先以 commit 粒度聚合近似“每日新增”
- **Likely codebase touchpoints:**
  - 新项目目录（待定）位于 `/Users/hong/Downloads/`
  - 数据源：`/Users/hong/Obsidian Notes/.git`
  - 统计参考：`/Users/hong/Obsidian Notes/.obsidian/plugins/novel-word-count/data.json`
